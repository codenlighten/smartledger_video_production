# HunyuanVideo deployment configuration

# Default prompt for quick tests
PROMPT="A cat walks on the grass, realistic style."

# Video generation settings
VIDEO_SIZE="720 1280"
VIDEO_LENGTH=129
INFER_STEPS=50
EMBEDDED_CFG_SCALE=6.0
FLOW_SHIFT=7.0
FLOW_REVERSE=true
SEED=42
USE_CPU_OFFLOAD=true
SAVE_PATH=./results

# Model paths (override if using custom weights)
DIT_WEIGHT=""

# Parallel inference (xDiT)
ULYSSES_DEGREE=8
RING_DEGREE=1

# Gradio server
SERVER_NAME=0.0.0.0
SERVER_PORT=7860
